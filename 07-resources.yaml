apivistion: v1
kind: Pod
metadata:
  name: resource-demo
spec:
  containers:
  - name: nginx
    image: nginx:1.25
    resources:
      limits:
        memory: "68Mi"
        cpu: "100m"
      requests:
        memory: "100Mi"
        cpu: "120m"