apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
  labels:
    project: roboshop
    components: frontend
    environment: dev
    purpose: node-port
spec:
  containers:
  - name: nginx-container
    image: nginx
    ports:
    - containerPort: 80
